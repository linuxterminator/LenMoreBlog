kind: pipeline
type: exec
name: default

platform:
  os: linux
  arch: amd64

steps:
  - name: 首次构建测试
    commands:
      - mkdir ~/build
      - echo "hello world"
      - touch hello.txt
      - mkdir hello
      - sudo systemctl status gitea.service
