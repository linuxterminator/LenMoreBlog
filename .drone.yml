kind: pipeline
type: exec
name: default

platform:
  os: linux
  arch: amd64

steps:
  - name: 首次构建测试
    commands:
      - pwd
      - cd ~
      - ls ~
      - sudo mkdir ./build
      - ls
      - pwd
      - echo "hello world"
      - sudo touch ~/hello.txt
      - mkdir hello
